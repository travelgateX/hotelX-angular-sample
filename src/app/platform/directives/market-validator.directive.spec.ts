import { MarketValidatorDirective } from './market-validator.directive';

describe('MarketValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MarketValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
