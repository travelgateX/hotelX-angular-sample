import { LanguageValidatorDirective } from './language-validator.directive';

describe('LanguageValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LanguageValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
