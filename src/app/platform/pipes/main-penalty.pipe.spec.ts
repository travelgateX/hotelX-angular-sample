import { MainPenaltyPipe } from './main-penalty.pipe';

describe('MainPenaltyPipe', () => {
  it('create an instance', () => {
    const pipe = new MainPenaltyPipe();
    expect(pipe).toBeTruthy();
  });
});
